syntax = "proto3";
package abacus.protobuf;
option go_package = "abacus/protobuf";

// Specifies the kind of metric
enum Kind {
    GAUGE = 0;     // Gauge metric
    COUNTER = 1;   // Counter metric
    CONSTANT = 2;  // Constant metric
}
